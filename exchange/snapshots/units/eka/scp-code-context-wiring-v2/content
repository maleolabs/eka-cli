{"objective":"Wire code-context into CLI and MCP while refining MCP into a small, safe, transport-neutral agent interface.","scope":"Implement shared code-context query path; CLI code-context \u003csymbol|file\u003e; CLI flags --depth local|dependency|engineering, --level 0..3, --no-content, --compact, --limit; direct MCP code_context with parity; deterministic ordering, cache invalidation, bounded output, fallback behavior, and parity tests. Refine MCP through one authoritative tool descriptor model, read/write risk classes, strict schemas, safer write policy, split publish modes, deprecated view handling, logical path outputs, and shared application API boundaries. Route draft_update through runtime-owned Authoring API with concurrency guard, or exclude it until available. Distribute EKA skills and command guidance primarily through MCP resources: compact manifest/index, bootstrap resource, lazy skill/template reads, version metadata, annotations, and fallback for clients without usable resource support. Keep commands as guidance/resources and operations as MCP tools; no manual per-agent installation as default.","outOfScope":"RAG/vector search implementation; RAG as primary source; full transitive call graph or LSP type resolution; code as CKO/docs/exchange artifact; unrelated product UI; remote MCP authentication implementation beyond defining local-trust boundary and policy seam; external feedback publishing redesign beyond safe isolation/disable-by-default.","acceptanceCriteria":"CLI/MCP code_context parity; defaults depth=dependency and level=1; levels 0..3 defined; invalid values deterministic; no-content refs-only; hard bounds; derived cache boundaries; tests for languages, misses/fallbacks, cache, ordering, output purity, token/precision. MCP read core explicit; advertised tools map to handlers and schemas; risk class visible to policy; schemas encode enums/bounds/mutual exclusion; deprecated view not advertised; absolute paths absent by default; high-impact writes require policy/approval; draft_update cannot bypass Authoring invariants; CLI/MCP share application APIs. MCP resources expose compact skill/command discovery and lazy content without requiring manual installation where client supports resources; resource loading behavior and fallback are documented/tested; protocol/client compatibility is explicit."}