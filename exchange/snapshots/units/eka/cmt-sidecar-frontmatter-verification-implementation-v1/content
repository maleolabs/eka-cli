{"changes":["research-only spike; no code changed"],"role":"implementation","summary":"Spike eka/spk:sidecar-frontmatter-verification complete — all four questions resolved against local installs + current official docs. V1 confirmed: opencode, Claude Code, and Codex scan only *.md in command dirs (Codex docs state 'ignores non-Markdown files' verbatim; opencode proven empirically — our .ekapack.json sidecar sits unharmed in ~/.config/opencode/commands/), so a DELEGATION.txt sidecar is invisible to every scanner. V2 confirmed: opencode 'agent:' frontmatter is optional (docs field table Required=No; defaults to current agent; multiple local commands run with description-only frontmatter) — renderer emits it only when the role-to-agent mapping resolves. V3 confirmed with a major Codex correction: conventional dirs are ~/.config/opencode/commands/ and ~/.claude/commands/, but Codex removed custom prompts entirely in codex-cli 0.117.0 (deprecated, replaced by skills at .agents/skills/), so the Codex sidecar goes INSIDE the installed skill subtree; proposal: DELEGATION.txt (.txt guarantees exclusion from *.md scanners), idempotent overwrite of pack-owned files on reinstall (matches existing pack.go os.WriteFile + plugin-installer overwrite-with-notice conventions). V4 refuted: Codex enables sub-agent workflows BY DEFAULT today (built-in agents default/worker/explorer; custom agents as TOML under ~/.codex/agents/; [agents] config block) — the solo-mode-only assumption for the Codex mapping table is outdated; named role-to-agent rows resolve on Codex only if the installer also ships TOML agent definitions, otherwise delegation stays dynamic/generic.","tests":["local config inspection: ~/.config/opencode/commands/ (+ .ekapack.json sidecar), ~/.config/opencode/agents/, eka-mcp pack.go installer semantics; ~/.claude/ and ~/.codex/ absent on this machine","official docs verification: opencode.ai/docs/commands + opencode.ai/v2/docs/commands (agent optionality, .md discovery); code.claude.com/docs/en/slash-commands + /en/claude-directory (commands/*.md, merged into skills, supporting files); developers.openai.com/codex/custom-prompts (prompts deprecated/removed 0.117.0, ignores non-Markdown), /codex/subagents (sub-agents default-on, built-ins, ~/.codex/agents/*.toml), /codex/skills (.agents/skills locations, SKILL.md + supporting dirs); openai/codex issues #15941/#15980 (prompt removal confirmation)"]}