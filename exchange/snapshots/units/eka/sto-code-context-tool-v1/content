{"description":"Serve Code Graph via code_context (CLI eka code-context + MCP) mirroring context semantics: depth local|dependency|engineering, opts --no-content/--compact/--limit, bounded 32 symbols / 64 units priority L0\u003eL1\u003eL2, L0 40-60L slice + import block max 10L, L1 deps as refs file:line:signature, L2 tests heuristic max 8 refs, L3 config lazy. Full body via code_get offset/limit with truncated handle. Deterministic sorted file:line, first-role-wins. Dynamic fallback: unresolved imports as string refs, test linkage heuristic, grep fallback bounded 20 tagged fallback-grep.","acceptanceCriteria":"code_context --depth dependency --no-content returns refs only (\u003c50% tokens vs file bodies), --depth dependency full returns L0 slice + L1 refs with bodies. E2 harness (12 tasks) shows token -72% (-78% refs) and p@1 +25pp vs file baseline, fallback overhead +12% max. Never sends raw AST. Tool grouping read/write/ops still holds."}