{"changes":["cmd/authoring.go: add --by and --by-kind flags to eka new; resolve via runtime.BySource (the same helper eka note/eka transition use); pass By in NewDraftRequest; unresolved identity -\u003e usage error (exit 2) with the pass --by \u003cname\u003e hint; document the flags in the command help","cmd/authoring_test.go: TestNewByDefaultsToGitIdentity covers the git-config default (changeLog by + author), the --by override, and the missing-identity refusal (exit 2, no draft left behind)"],"role":"implementation","summary":"eka new now resolves the change-log authority (by) from git config user.name, overridable with --by — matching eka note and eka transition (BySource). Previously eka new scaffolded drafts with the by: Engineering placeholder. Root cause: cmd/authoring.go (eka-cli) never passed NewDraftRequest.By, so eka-core's draft.go fell back to User(\"Engineering\"). Fix is CLI-side: new --by/--by-kind flags + runtime.BySource resolution; unresolved identity is a usage error (exit 2), never a silent placeholder. eka-core unchanged: NewDraftRequest.By plumbing already existed; the Engineering default remains reachable only for library callers.","tests":["go test ./... eka-cli: PASS (all packages)","go test ./... eka-core: PASS (unchanged, no code change)","TestNewByDefaultsToGitIdentity: PASS","smoke: eka new dec:x -\u003e changeLog by == git config user.name (Smoke Tester)","smoke: eka new sto:y --by Foo -\u003e by == Foo","smoke: unresolved git identity -\u003e exit 2 with pass --by \u003cname\u003e"]}