{"description":"Decouple eka-mcp configure from skills/commands install. Register to agent should only write MCP client config; installing skills and commands must be opt-in via flags because both are already accessible via MCP resources (eka://skills/*, eka://templates/*) and tools.","acceptanceCriteria":"eka-mcp configure --target opencode --dir . --json by default only writes MCP entry (no filesystem skill/command copy); flags --with-skills, --with-commands, --with-all (or --with-skills --with-commands) opt-in install; --dry-run reflects plan correctly; existing behavior with flags preserves checksum and idempotency; docs updated."}