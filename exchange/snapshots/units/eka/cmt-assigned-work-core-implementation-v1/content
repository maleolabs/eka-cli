{"changes":["conformance: add the mbr- member token (28th type token; Execution operating token owning content-state + existence-state; RequiredSections Purpose/Content; R10 exemption joins tkt/ses/cmt) — typeTokens, tokenDomain, requiredSectionsFor, rule10","conformance: add the assigned-to relationship field (8th canonical field) to relationshipFields — R5 resolution + draft-target tolerance inherited from the shared R5 mechanics; the exchange importer accepts it via RelationshipFieldNames","conformance: implement the conditional R13 assigned-to sub-check in the graph gate (conformance/graph.go): binds only work items at in-review/done carrying assigned-to; checks target resolves to an mbr- line, at-most-one target (single-assignee), and repository-level provenance equality (cross-repository assignment refused); work items without the field produce zero findings (legacy untouched)","view: WorkItem gains the Assignee field (canonical line identity form of the resolved mbr- line, derived from the assigned-to relationship only — ADR-013)","view: add Graph.WorkItemsForMember(form) — member-scoped work item retrieval, relationship-only membership, sorted by canonical identity","view: add BoardForMember(g, memberForm) — member-scoped board with the dedicated 'No assignee' bucket (items without the assigned-to edge surface there, never silently excluded; ADR-029 Decision 3); repository-wide board unchanged","tests: 16 new unit tests — mbr- vocabulary acceptance, assigned-to R5 acceptance + CKO draft-target tolerance, conditional R13 sub-check (legacy zero findings, valid assignment, non-member target, unresolvable target, cross-repository provenance refusal, at-most-one, gate-only binding, deterministic ordering), view assignee resolution, WorkItemsForMember filter, member board filter + 'No assignee' bucket semantics, byte-deterministic output"],"role":"implementation","summary":"Conformance vocabulary (mbr- token + assigned-to relationship, R10 exemption) and the conditional R13 assigned-to sub-check (typed target, single-assignee, repository provenance; legacy data untouched), plus store-backed view projections: assignee resolution on work items, WorkItemsForMember, the member-scoped board with the 'No assignee' bucket.","tests":["go test -race -count=1 ./... — 688 passed, 0 failed (was 672; 16 new tests)"]}