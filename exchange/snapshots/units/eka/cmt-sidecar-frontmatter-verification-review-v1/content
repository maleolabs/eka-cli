{"notes":["Review of eka/spk:sidecar-frontmatter-verification:1 (research spike, in-review) against eka/req:agent-agnostic-skill-pack baseline (R4 codex/default solo mapping, R8 explicit solo degrade, Deferred V1-V4). Verdict: APPROVE - all four verdicts in the evidence note are sound and were independently re-verified by the reviewer.","Checklist 1 (note exists, resolved, contains per-V verdicts): SATISFIED WITH CAVEAT. Reviewer session had no eka CLI/MCP execution path (no shell tool; eka MCP server not connected; canonical store ~/.eka/eka.db is binary SQLite, unreadable read-only here). Existence of a resolved implementation note is inferred from the R13 transition gate (spike unit.json shows executionState advanced planned-\u003etodo-\u003ein-progress-\u003ein-review on 2026-08-23, which the CLI only permits with a resolved implementation note) plus mutually consistent staging files /tmp/opencode-marij/spk-note.json and /tmp/opencode-marij/spk-findings.json containing the full per-V verdicts and evidence. Orchestrator should confirm once via: eka get eka/spk:sidecar-frontmatter-verification --downstream.","Checklist 2 (local spot-checks): PASS. ~/.config/opencode/commands/ contains non-.md sidecar .ekapack.json coexisting with 20 working .md commands (V1 local proof reproduced). chore.md carries description-only frontmatter while eka-discuss.md carries 'agent: alex' - both forms load (V2 local proof reproduced). ~/.claude/ and ~/.codex/ confirmed absent on this machine, matching the note's own admission that Claude Code/Codex claims rest on official docs only.","Checklist 3 (consistency with req + orchestrator decision): CONSISTENT. V4's refutation (Codex ships sub-agents by default) does not contradict keeping option (a) codex=solo/all-self: solo remains the safe conservative default, satisfies R8 trivially, and capability utilization is a scope matter properly deferred to a planning revision. SAFETY CONDITION for option (a): the renderer/mapping tables must emit NO named-agent rows for codex (named rows resolve only if installer also emits ~/.codex/agents/*.toml definitions), and shipped bodies/docs must not claim 'Codex has no sub-agents' (outdated) - phrase as 'solo by default; native sub-agents exist, named mapping deferred'. Feed to ts:mapping-tables, ts:solo-mode-degrade, ts:installer-render-sidecar.","Checklist 4 (high-confidence claims on weak sources): NO BLOCKING CASE. V3's load-bearing Codex correction re-verified live: openai/codex issue #15941 confirms maintainer statement 'custom prompts ... removed completely starting in 0.117.0' (corroborated by #15980); developers.openai.com/codex/subagents confirms verbatim 'Current Codex releases enable subagent workflows by default', built-ins worker/explorer/default, and custom agents as TOML under .codex/agents/. V2 re-verified: opencode docs state agent config is optional ('Use the agent config to optionally specify which agent should execute this command'). Residual asymmetry (low): empirical local proof covers opencode only; Claude Code/Codex behavior rests on vendor docs - acceptable for high confidence, but implementation tickets should include a smoke test on real Claude/Codex installs when available.","Installer claim code-verified: eka-mcp/pack.go installTo uses os.MkdirAll + plain os.WriteFile overwrite scoped to pack-owned names only (CommandFiles()/skill manifest), never touching foreign files - supports V3's idempotent-reinstall conclusion; skills install as directory subtrees (copyTree), so a DELEGATION.txt inside a skill subtree flows through the same mechanism (relevant to the Codex placement recommendation).","Minor (low): in the exchange snapshot, spk-sidecar-frontmatter-verification-v1 content keys (description/investigationNotes/conclusion) are empty; evidence lives solely in the cmt note. If the canonical spk object is likewise empty, either fill the spk conclusion at the done-transition or explicitly designate the cmt note as the evidence carrier so the spike line is not an empty shell.","Process note: reviewer could not run eka validate / eka integrity check / eka note / eka publish in this session (no execution tooling; MCP resources/templates empty). Review JSON prepared at /tmp/opencode-marij/spk-review.json; publishing steps must be executed by the orchestrator: (1) eka note eka/spk:sidecar-frontmatter-verification --role review --content-file /tmp/opencode-marij/spk-review.json ; (2) set noteState resolved (+ changeLog entry) in the new draft under ~/.eka/drafts/eka/ ; (3) eka publish eka/cmt:\u003cnote-id\u003e ; (4) verify downstream noteState=resolved."],"role":"review","verdict":"approve"}