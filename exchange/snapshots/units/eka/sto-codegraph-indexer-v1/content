{"description":"Package eka-core/codegraph (Runtime consumer like contexts) with tree-sitter Go/TS/Python (smacker/go-tree-sitter pure-Go fallback), import graph from import_statement nodes, file → [{name,kind,start,end,signature,fileHash}] index, per fileHash cache + global gitDigest (git rev-parse HEAD) invalidation, store ~/.eka/cache/\u003cproject\u003e/codegraph-\u003cgitDigest\u003e.json (~1.2MB), hook in eka sync diff fileHash → re-parse changed files only (cold 1k ~1.8-2.4s, incremental 10 files 45-90ms, determinism ≥95% as measured spk).","acceptanceCriteria":"go test ./codegraph passes for Go/TS/Python fixtures (named defs + imports). fileHash cache rebuild cold \u003c5s/1k files, incremental 10 files \u003c150ms measured. Determinism 98.7% same content → same spans/edges. Store not a CKO, not in snapshot, gitignored. Python fallback not required here (next item)."}