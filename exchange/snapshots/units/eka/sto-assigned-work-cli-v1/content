{"description":"eka-cli surface for the 'Assigned Work' slice: assign/unassign/reassign commands as thin wrappers over the assigned-to edge, and the advisory --member board filter with assignee tags, 'No assignee' bucket, terminology help, and machine-readable output keys.","acceptanceCriteria":"Commands eka assign \u003citem\u003e --to \u003cmbr\u003e, eka unassign \u003citem\u003e, eka reassign \u003citem\u003e --to \u003cmbr\u003e work per the locked semantics (assign idempotent on same target, refusal on different target; reassign single-operation move with refusal when unassigned; unassign no-op exit 0 when absent); exit codes 0/1/2; provenance check refuses cross-repo targets; unresolvable ids refuse with member list. eka view board --member me|\u003cmbr-id\u003e filters deterministically (me resolves via git config user.name to exactly one mbr-; zero/multiple match = deterministic refusal listing members), renders assignee tags, the separate 'No assignee' bucket (visible, never hidden), filtered summary counts, suppressed container-axis 'Unassigned' insight in member views, and pinned machine keys (assignee, no-assignee); help adds the terminology line distinguishing 'No assignee' (member axis) from 'unassigned' (container axis); byte-deterministic output preserved."}