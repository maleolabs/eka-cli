{"changes":["internal/eka/refusal.go (new): wrapToolRefusal detects structured error classes via errors.As (*runtime.PublishError, *runtime.RelateValidationError, *runtime.TransitionRefusal) and wraps them into toolRefusal — Error() delegates verbatim (headline stability), Unwrap() preserves the chain, accessors expose RefusalReport/RefusalWarning/RefusalConfirmation; marshalConformanceReport reuses the exact wire structs as the validate tool so eka-conformance-report-v1 shape holds by construction; per-field path redaction on root/file/message","internal/mcp/server.go: toolFailureResult unwraps via errors.As against a local refusalFidelity interface (keeps server stdlib-only, decoupled from eka-core); renders a SECOND text content block with the full report JSON (block 0 headline stays byte-stable by construction); transition refusals additionally surface warning banner + literal 'retry with confirmed:true' affordance when Confirmation is set; plain refusals keep the historical single-block shape","path redaction two-layer: serializer redacts per field; boundary guard re-applies RedactPaths to the whole report block (regex excludes quote/colon/newline so JSON validity preserved; idempotent)","skills/eka-troubleshooting/SKILL.md: new section 'MCP tool refusals carry the full report' + cross-refs in validation and membership-gate classes"],"role":"implementation","summary":"Implemented sto:mcp-error-fidelity on branch feat/sto-mcp-error-fidelity (commit 11e8fcc, base origin/develop 4a60d02) in eka-mcp. Tool execution failures now unwrap structured errors at the MCP boundary and embed the FULL conformance report inline instead of the count-only summary; transition refusals surface their Warning and the confirmed:true retry affordance that was silently dropped before. Coverage is class-level at the boundary: PublishError wired explicitly (publish), RelateValidationError covered by class (no relate MCP tool exists today — verified in toolNames), TransitionRefusal covered (transition incl. container pipeline); note-resolve refusals flow through the same wrap chain. Rendering choice: second text content block (spec-valid on 2024-11-05 baseline, no structuredContent dependency) keeping block 0 headline byte-stable by construction. No GitHub issue exists for this item (tracked in EKA only, owner decision rev 2).","tests":["go build ./... ok; go vet ./... clean; gofmt -l . empty; go test ./... -count=1 = 299 passed / 0 failed (283 existing pinned suites untouched + 16 new)","internal/mcp/fidelity_test.go (8): full report embedded per-finding rule/severity/message incl warnings; headline == SanitizeError verbatim (byte-stable); no path/store leakage inside findings while identity forms survive and JSON stays valid; transition confirmation affordance surfaced; plain transition refusal stays single-block; plain tool error stays single-block; carrier without report adds no block; fidelity unwraps through wrapped error chains","internal/eka/refusal_test.go (8): wrapper behavior for all three error classes (verbatim message, Unwrap, sorted results, counts); serializer redaction; nil-report pass-through; detection through note-resolve %w chains; end-to-end publish of a draft with unresolved reference yields carrier with report findings and draft intact"]}