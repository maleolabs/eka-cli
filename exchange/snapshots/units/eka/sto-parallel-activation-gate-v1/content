{"description":"Replace the project-wide exactly-one-active check in eka-core/runtime (otherActiveContainer + transitionContainerState activation path, container.go protocol comments) with one-active-per-source_repo plus a transitive depends-on/derives-from closure-disjointness check over plan lines.","acceptanceCriteria":"1. Activating a planned container while another container is active in the SAME source_repo refuses with the exactly-one-active message (existing behavior preserved). 2. Activating while another container is active in a DIFFERENT source_repo with disjoint plan closures succeeds. 3. Cross-repo activation whose transitive plan closures share a node refuses naming the shared plan/edge. 4. Store-read failure still fails closed. 5. Unit tests cover same-repo refuse, disjoint parallel allow, shared-node refuse, cycle-safe closure."}