{"notes":["minor: skills/scripts/smoke-test.sh section 8 (lines 126-139) still drives `$EKA_PATH install skills|commands` — the eka-cli surface this very changeset declares nonexistent (manifest.yaml: 'eka-cli exposes no install surface'; installation.md: '`eka install` no longer exists'). This breaks the pack's own documented validation path (installation.md section 8 and manifest.yaml maintenance.verifyPack both point at this script). Pre-existing file outside the diff, so not blocking, but it is a live internal contradiction in skills/** after this cleanup — recommend a follow-up work item porting those assertions to `eka-mcp configure --dry-run --json`.","minor: skills/README.md line 95 cites the official path as inline code `eka-mcp configure --with-skills` without the REQUIRED `--json` flag — run verbatim it refuses ('configure: missing --json', enforced at configure.go lines 85-87/226-228). All other surfaces (manifest, installation.md, commands/README, root README) include --json. Same line's trailing clause 're-running install fixes them' references a verb ('install') that no longer names any command. Wording-only, deterministic error message limits harm.","info: installation.md documents `--with-all` installs (which write DELEGATION.txt) but never mentions the sidecar or its reserved-name status; the reserved-name warning lives only in root README (lines 89-93) and commands/README (line 35). Completeness nit, not a contradiction.","info: manifest installCommand summary line omits auxiliary --dir/--dry-run flags; they are documented in installation.md, commands/README and root README with matching semantics. Summary-line omission, no contradiction.","verified: manifest installCommand matches cmd/eka-mcp/configure.go reality (--target opencode|claude|codex, --with-skills|--with-commands|--with-all, --json required, codex command refusal); installation.md count 'eleven' equals the 11 actual skill dirs and 11 manifest entries; only remaining 'eka install' mention in skills/**+README.md is the intentional deprecation note (installation.md line 21); root README clean.","verified: TestRoleContractMatchesMappingTables (+91 lines, only change to pack_test.go) genuinely fails on drift in both directions — bodies' extracted role contract must equal sorted RoleVocabulary AND every embedded table must equal it, anchored on RoleVocabulary so even identical body+table drift is caught; extraction traced against both real bodies (heading '## Role contract\\n' unique, section bounded by next '\\n## ', header/separator rows filtered, 9 roles recovered from each); missing section or zero rows t.Fatalf instead of passing vacuously; want-slice copied before sort so the global is not mutated. Only theoretical false-positive is an extra table added inside the section, which fails loudly — correct behavior for a drift guard.","verified: invariants hold — zero alex-/althea- tokens in skills/commands/*.md (agent names confined to mappings/*.toml and tests); both command bodies description-only frontmatter; Transport primitives / Skill loading / Delegation mode sections byte-identical vs baseline (eka-discuss.md head-to-head compared, eka-execute.md heading positions/frontmatter/line counts match; 161/222 lines both trees).","verified: security-minor fixes accurate and consistent — symlink wording ('final path component refuses; intermediate directory symlinks are followed') matches code exactly (classifyTarget uses os.Lstat on the final component; MkdirAll/CreateTemp follow intermediate dirs; rename replaces rather than follows) and is worded consistently across root README and all three reworded install.go comments; DELEGATION.txt reserved-name documentation present where disk installs are documented.","verified: scope discipline — changed files are exactly manifest.yaml (line 13), skills/docs/installation.md (sections 2/5), skills/README.md (line 95), README.md (reserved-name note, symlink precision, new Mapping burden section), skills/commands/README.md (--json examples, reserved name, rendered-at-install wording), install.go (comment-only: 3 doc-comment blocks reworded, every function body byte-identical), pack_test.go (+91). Untouched pairs confirmed via line-count + spot content: pack.go (587), install_test.go (450), cmd/eka-mcp/{main.go 186, main_test.go 433, configure_test.go 565}, mappings/*.toml, workflow.md (149), CHANGELOG.md. Removal of the '.ekapack.json' state-marker sentence from installation.md is correct — no such marker exists anymore (only the unrelated .ekapack-* temp prefix).","verified: cross-doc consistency — targets/layouts/default target/codex refusal agree across root README, commands/README, installation.md, configure.go help text and ResolveLayout; create|overwrite|skip action names match FileAction; the new Mapping burden section accurately describes the M3 test's actual semantics; no stale '--target all'/auto-detect claims remain."],"role":"review","verdict":"approve"}