{"objective":"Implement safe symlink handling during code discovery index building","scope":"Fix code-discover, code-get, code-context commands to follow symlinks safely using cycle detection and expose a CLI flag `--follow-symlinks=true|false` (default true). Update CLI usage docs and validation tests.","outOfScope":"Changing other file walking behavior (e.g., handling of broken symlinks, not inspecting entire repos, adding generic symlink support for all commands beyond codegraph, adding non-symlink index scanning algorithms)."}