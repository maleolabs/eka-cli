{"objective":"Scope the plugin install + trust feature for eka-cli and eka-mcp.","scope":"In scope: hardcoded official registry (eka-cli/plugin/registry.go); `eka plugin install/list/remove/update`; checksum-verified download from GitHub releases; two-tier trust (official auto / third-party consent); manifest `capabilities` (install + mcp) and `source`; eka-mcp release binaries + SHA256SUMS.","outOfScope":"Out of scope: binary signing (cosign/minisign), plugin sandboxing, a remote registry, run-time trust enforcement, and any MCP server capability implementation beyond the capability declaration."}