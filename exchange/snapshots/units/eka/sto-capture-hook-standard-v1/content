{"description":"Sebagai maintainer EKA, saya ingin git hook pre-commit/pre-push terdistribusi via eka-standard + anvil sehingga direct commit tanpa API tetap ter-capture sebagai draft reconciled di semua clone.","acceptanceCriteria":"- Template hooks pre-commit \u0026 pre-push di eka-standard/templates/hooks di-commit\n- anvil install fase platform_sync symlink hook ke .git/hooks\n- Hook jalankan `eka capture --reconcile --from-git` baca git diff --cached / log --since=24h\n- Generate draft provenance=reconciled, sourceCommitSha, confidence 0.5 jika signifikan tanpa draft ter-link\n- Update hook terdistribusi via anvil update / bump eka-standard\n- E2E test di CI fixture: git commit tanpa draft -\u003e muncul reconciled draft"}