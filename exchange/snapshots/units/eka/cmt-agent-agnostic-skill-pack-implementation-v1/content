{"changes":[],"role":"implementation","summary":"Planning discussion 2026-08-15 on eka/fnd:agent-agnostic-skill-pack. Sub-agent analysis: alex-architect (mechanism design — read command bodies, README, pack structure, ADR-022/023) and althea-product-specialist (value/priority — verified source state: 11 skills + 2 commands, 3 MCP tools read-only, pack not MCP-reachable). Agreement on core mechanism: role-based delegation contract in command bodies (9 stable roles with input/deliverable/escalation, never concrete agent names), per-ecosystem mapping layer (mappings/{opencode,claude,codex,default}.md pre-rendered, installed as DELEGATION.md sidecar, installer pure-copy), explicit solo-mode degrade path (no sub-agents =\u003e primary performs roles inline, recorded as mode: solo, never silent; /eka-execute primary-never-implements rule becomes conditional). Architect additions: canonical body carries only description frontmatter, provider frontmatter rendered by installer; skills dual path (file install + MCP resources eka://skills/\u003cname\u003e — already listed in req:eka-mcp-production); skill reference load-order protocol (installed dir, then MCP resource, else inline hard rules); transport-neutral body (CLI and MCP tools as equal paths). Product additions: value ranking = standard credibility \u003e new ecosystems (Claude/Codex) \u003e status quo (external opencode adopters without alex-* agents also break today); framing 'one body, declarative role contract, per-ecosystem mapping'; MVP = both commands, 3 ecosystems; non-goals = commands as MCP tools (req locked), per-ecosystem harness; priority = follow-up after mcp-production, same release train as release-coordination; risks = taxonomy bloat, silent degrade, body vagueness, opencode regression (mitigated: opencode+maleolabs = default reference mapping + smoke tests). User decisions (2026-08-15): skills = dual path (architect position); solo-mode execute = proceed + record; priority = follow-up post MCP (product position). Open items recorded in fnd: opencode frontmatter-optionality, Claude Code DELEGATION.md sidecar scan behavior, frontmatter render ownership (CLI vs plugin), per-skill transport-neutral note, docs skill-count sync (11 vs 10).","tests":[]}