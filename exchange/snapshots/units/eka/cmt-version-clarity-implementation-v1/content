{"changes":["eka-core metadata: exported SchemaVersion constant as the single source of the eka.yaml schema version; Parse now accepts exactly metadata.SchemaVersion","eka-cli cmd/version.go: standardVersion derives from exchange.SpecificationVersion (const, no hardcoded literal); new --json flag emits the six-axis report (versionInfo struct, fixed key order, indent + newline); plain output keeps the historical two-line head and extends to all six axes; help documents the axes","eka-cli bootstrap: generatedEkaYAML and the reported identity write metadata.SchemaVersion instead of the literal version: 1","eka-cli go.mod: local replace of eka-core to the companion feature branch (metadata.SchemaVersion does not exist in the published v1.1.0); the replace must be dropped/repointed when the core change lands in a release"],"role":"implementation","summary":"Clarified the eka version axes: `eka version --json` now reports all six axes (standard corpus, RSF serialization, exchange spec, machine schema, eka.yaml schema, CLI), each derived from a single canonical source in eka-core; standardVersion is no longer hardcoded in the CLI.","tests":["eka-core metadata.TestSchemaVersionConstant: SchemaVersion is accepted by Parse and SchemaVersion+1 is refused","eka-cli cmd.TestVersionStandardDerivesFromCore: standardVersion equals exchange.SpecificationVersion (single source locked)","eka-cli cmd.TestVersionJSON: all six axis keys present, values match the eka-core constants, output deterministic","eka-cli cmd.TestVersionPlainAxes: plain output keeps 'eka \u003cver\u003e' and 'EKA standard \u003cstd\u003e' and adds every remaining axis","eka-cli cmd.TestVersionJSONFlagHelp: --json is documented"]}