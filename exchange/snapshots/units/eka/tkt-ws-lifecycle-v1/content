{"commands":"\u003e Generated — State Projection. Do NOT edit state here; refresh on read.\n","projectedStatus":"","acceptanceCriteria":"1. eka-core: Workspace.UnregisterRepo(projectID, name) deletes the repo row; refuses the reserved name 'runtime'; reports whether the row existed; when it was the project's last repo the empty project row is removed too (documented). Exposed via runtime WorkspaceService. 2. eka-cli: `eka project remove \u003cproject\u003e/\u003cname\u003e` removes the registration, prints the removed repo + path, notes that canonical knowledge objects remain in the workspace store (re-registering restores provenance access); unknown project/repo exits 2 with a candidate list. 3. Unit tests both sides; go vet PASS; existing register/list tests unchanged.","description":"Ticket for eka/sto:ws-lifecycle — complete the workspace registry lifecycle: `eka project register` and `eka project list` exist, but a registered repository can never be removed. Add `eka project remove` (unregister) with deterministic semantics."}