{"description":"Fix project-scoped execution state retrieval for `eka get ses:execution-state` and `eka status` execution section. Currently both leak the legacy EKA project state when invoked from other registered projects.","acceptanceCriteria":"1. `eka get ses:execution-state` resolves execution state for current project/namespace. 2. `eka status` execution section displays only current project's execution state. 3. If no project-local SES exists, show project-scoped not-found. 4. Legacy fallback only when current repo IS the legacy EKA project. 5. Tests pass with two registered projects with distinct SES records."}