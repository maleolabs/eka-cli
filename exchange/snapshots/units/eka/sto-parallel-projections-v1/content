{"description":"Make eka-cli projections multi-active aware: render_execution.go (drop anomaly warning for genuine multi-repo actives), view board/containers/get containers filters, and per-repo active in status output.","acceptanceCriteria":"1. view execution with N active containers renders one board per container plus a summary (no anomaly warning). 2. view board/containers and get containers --active/--current handle multiple actives deterministically. 3. eka status shows per-repo active container (repo-scoped, ADR-036). 4. Single-active rendering unchanged when only one container is active. 5. Projection tests cover 0/1/N active states."}