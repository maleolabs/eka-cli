{"changes":["feat: eka --version persistent root flag — prints the same first line as 'eka version' (ldflags cmd.version source, identical output byte-for-byte); landing page points at it","docs: eka get / eka get ticket / eka view ticket help now document the tkt- generated state projection constraint (Generated — State Projection. Do NOT edit state here; never edit a projection's state — transition the referenced work item instead)","docs: eka new and eka relate relationship flag help now explicitly state 'comma-separated values and repeated flags accumulate' (previously only the Long help mentioned accumulation)","test: --compact output pinned for every unit type (adr, epc, plan, trc, scp, sto, ts, tkt, bug, ch, cmt, ctr), all five domains, containers, empty collection and the ticket subcommand — single-line, non-empty, parse-equal to pretty"],"role":"implementation","summary":"CLI polish (eka/sto:cli-polish) implemented: eka --version flag added; eka get --compact verified working for every target type (no missing/gated renderer found — behavior pinned by regression tests); hidden constraints (ticket projection header, relationship comma-join) made discoverable via help text.","tests":["go test ./... — 560 passed, 0 failed","gofmt -l . — empty","go vet ./... — clean","smoke: eka --version output identical to first line of eka version (also with -ldflags version injection); eka get --compact non-empty on all affected types; help greps for the new constraints"]}