{"description":"Add work-item assignment coverage to the EKA AI skill pack: the assign/reassign/unassign operations (CLI today, MCP write tools once ts:mcp-assignment-tools lands) are absent from every SKILL.md, leaving multi-agent collaboration flows undocumented. Update the relevant skills — primary: eka-engineering-workflow (assignment belongs to the execution loop's item-intake step); secondary touch if natural: eka-knowledge-modification (state-context for assignment changes).","acceptanceCriteria":"1. Assignment operations documented where the execution loop is taught: what assign/reassign/unassign do, valid targets and states, refusal cases, and how assignment interacts with container membership (tickets remain the membership contract — assignment never creates membership).\n2. Both transports covered honestly: CLI commands as they exist today; MCP tools referenced via the load-order/availability reality-check pattern (cite only surfaces that exist at ship time — if mcp tools are not yet shipped, mark them pending, never phantom-cite).\n3. Pack invariants intact: zero alex-/althea- tokens, no ecosystem names hardcoded, manifest skill-count unchanged unless a new skill is added (prefer editing existing skills over adding one).\n4. bumpPolicy honored: pack version bump evaluated per manifest bumpPolicy since command-surface references change.\n5. Full gates green in eka-mcp: go build ./..., go vet ./..., go test ./... (includes TestRoleContractMatchesMappingTables drift guard).","debtRationale":""}