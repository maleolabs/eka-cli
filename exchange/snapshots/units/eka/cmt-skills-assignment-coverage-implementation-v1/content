{"changes":["skills/eka-engineering-workflow/SKILL.md — new 'Assignment — claiming work at item intake' subsection between Change and Validate: what each op does (set first assignee idempotent-same-member / move in one op / remove edge), valid targets (work items only sto-/ts-/bug-/td-/ch-/spk-, any execution state, published or pending draft, no instance churn; member must be a resolvable mbr- line of the SAME repository), deterministic refusal cases (assign onto different member, reassign with no assignee, unresolvable member with known lines listed, non-work-item target, cross-namespace item, validation findings; exit 0/1/2), and the critical clarification that assignment NEVER creates container membership (tickets are the sole membership contract; member axis 'No assignee' vs container axis 'unassigned').","skills/eka-engineering-workflow/SKILL.md — Ticket execution flow row now carries an explicit intake step: align the assignee on the same `eka get` read (claim via assign when unassigned, reassign only as explicit handoff); new Common-workflow-mistakes bullet on treating assignment as container membership; frontmatter description extended with work-item assignment for routing/discovery.","skills/eka-knowledge-modification/SKILL.md — assignment-edge change path added to the sanctioned-paths table (no-churn edge write, distinct from transitions and revisions) plus a short paragraph; heading 'Two sanctioned change paths' renamed to 'Sanctioned change paths' since a genuine third runtime path now exists (no cross-references to the old heading).","Transport honesty (AC2): both skills document the CLI as-is (--json schema eka-assignment-v1, --by/--by-kind authority) and mark MCP assignment tools PENDING — verified against internal/mcp/server.go handleToolsList that today's server exposes context/get/domain/status/validate/new/publish/transition/note/view/draft_list/integrity_check/discard only, no assignment tools; phrased via the availability reality-check pattern (re-verify against the server's tool list), never phantom-cited.","skills/manifest.yaml — pack.version 1.0.0 -\u003e 1.0.1 per bumpPolicy ('pack version bumps when the skill set, command surface references, or templates change'): command-surface references changed, no skill added/removed, templates untouched. CHANGELOG.md [Unreleased] entry documents the bump and coverage.","Pack invariants (AC3): manifest skill-count unchanged (no new skill), zero alex-/althea- tokens in added lines, no hardcoded ecosystem names in added lines (grep-verified)."],"role":"implementation","summary":"Documented work-item assignment operations (assign/reassign/unassign) in the EKA AI skill pack per ACs of eka/ts:skills-assignment-coverage. Primary target eka-engineering-workflow gained a dedicated Assignment section at the execution loop's item-intake step; secondary touch landed naturally in eka-knowledge-modification as a third sanctioned change path. CLI semantics verified against `eka assign/reassign/unassign --help` on eka v1.4.3 before writing.","tests":["go build ./... — OK (GOCACHE=/tmp/opencode-marij/go-cache GOTMPDIR=/tmp/opencode-marij)","go vet ./... — OK","go test ./... — ok github.com/maleolabs/eka-mcp, ok .../cmd/eka-mcp, ok .../internal/eka, ok .../internal/mcp (includes TestRoleContractMatchesMappingTables drift guard); re-run green after final heading rename","Invariant grep over added diff lines: zero alex-/althea-/ecosystem-name hits"]}