{"description":"Wire B1 deferred registration for eka-mcp: emit commands in manifest so eka-cli registers plugin commands via deferred registration (ADR-031) without PATH. User expects eka mcp / eka mcp serve proxied via eka-cli, not via shell PATH.","acceptanceCriteria":"eka-mcp manifest --json emits B1 commands array (e.g. {name:\"mcp\", description:\"EKA MCP server\", args:[\"serve\"]}); eka --help shows Plugins group when plugin installed; eka mcp dispatches to eka-mcp with G2 hash verify, bounded env, exit code propagation; PATH-only still refused (G3); collision and probe timeout semantics unchanged."}