{"objective":"Implementation plan for the cli-experience scope: init installs the embedded EKA standard declaration; help output grouped by intent and colored.","scope":"Ordered by dependency: 1. sto:init-standard-declaration — amend ADR-020 (Decision 1 carve-out), vendor the EKA file from the eka-standard release asset into an embed package (root-level, //go:embed precedent), add the bootstrap ActionGenerateEKA (plan order: dir → eka.yaml → EKA → git → validate; reuse/overwrite-confirm and already-initialized backfill semantics), derive standardVersion from the embedded file (cmd/version.go, ~13 render sites), add the build-time version-consistency test. 2. sto:help-ux — cobra AddGroup/GroupID as the data model, custom help renderer shared with the landing (renderCommandGroups), colors TTY-only via the ui.Style paint path, subcommand help colored only, assertNoANSI extended to help paths, unit tests for group membership and color rendering.","outOfScope":"Network fetch at init; --standard-version / --offline flags; checksum verification of the standard asset; the full `eka version --json` multi-axis surface (sto:version-clarity remains a separate story); grouping in subcommand lists; eka.yaml schema changes; changes to eka-standard release automation (release.yml already correct)."}