{"changes":["skills/scripts/smoke-test.sh: section 8 ported from the removed `eka install` surface to `eka-mcp configure` (ADR-030). New EKA_MCP_PATH variable (default eka-mcp) plus environment checks for the eka-mcp binary and jq.","skills/scripts/smoke-test.sh: 8a dry-run assertions — exit 0, valid JSON (jq -e), dryRun=true, counts.created \u003e 0 with overwritten/skip = 0 on a fresh fake HOME, every changes[].action in {create,overwrite,skip}, counts sum == changes[] length, DELEGATION.txt sidecar present in the plan, and zero filesystem writes.","skills/scripts/smoke-test.sh: 8b real install into a temporary fake HOME (mktemp -d; HOME + cwd both anchored there) — exit 0, valid JSON, created count equals the dry-run plan (plan parity), installed.skills contains eka-feedback/eka-troubleshooting, installed.commands contains eka-execute.md, SKILL.md + templates + command file exist on disk, DELEGATION.txt sidecar present next to commands with a non-.md extension, opencode.json MCP client entry written.","skills/scripts/smoke-test.sh: 8c idempotency — second identical run exits 0 with created=0, overwritten=0, skipped == first-run created, and all changes[].action == skip.","skills/scripts/smoke-test.sh: 8d codex refusal — configure --target codex --with-commands --json exits non-zero with the deterministic refusal message, identical output across two runs, and no .agents tree written. Scratch CFG_HOME removed during cleanup."],"role":"implementation","summary":"Ported smoke-test.sh section 8 from the removed `eka install` surface to the current `eka-mcp configure --dry-run/--with-all --json` surface per ADR-030. The section now drives a dry-run plan check, a real install into a temporary fake HOME, an idempotent re-run, and the deterministic codex command-install refusal — all asserted through jq against the machine-readable JSON (exit codes, changes[] actions, counts {created|overwritten|skipped}, DELEGATION.txt sidecar presence and non-.md extension). No pack content files touched: only skills/scripts/smoke-test.sh changed.","tests":["bash -n skills/scripts/smoke-test.sh — syntax OK","grep for `$EKA_PATH\" install` / `eka install ` invocations — clean (AC 1)","git status — only skills/scripts/smoke-test.sh modified; skills/** content and mappings/** untouched (AC 5)","Section 8 executed inside the real script machinery against built binaries (/tmp/opencode-marij/bin/eka + eka-mcp): all 27 configure assertions PASS — dry-run plan (43 creations, actions valid, sidecar planned, nothing written), install parity (43 created, artifacts + sidecar + MCP config on disk), idempotent re-run (43 skips), codex refusal (exit 1, deterministic, no writes) (AC 2, 3)","Full-script run: 40 passed / 26 failed — every failure is a pre-existing fixture-dependent assertion (sections 1-7, 9 require the Feather Reference Project at reference/project, which is absent from this machine and from both repositories' git histories); failure set unchanged by this item","Gates in worktree (GOCACHE=/tmp/opencode-marij/go-cache GOTMPDIR=/tmp/opencode-marij): go build ./... OK, go vet ./... OK, go test ./... → ok github.com/maleolabs/eka-mcp, ok .../cmd/eka-mcp, ok .../internal/eka, ok .../internal/mcp"]}