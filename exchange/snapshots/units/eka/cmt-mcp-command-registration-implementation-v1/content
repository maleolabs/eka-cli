{"changes":["cmd/plugin_registration.go (new): registerPluginCommands + pluginRegEntryFor (per-process memoized cache, TTL + binary identity), probePluginManifest (bounded subprocess, env whitelist, 1 MiB output cap), pathOnlyRefusals (G3), installedOfficialNamesIn, newPluginDispatchCommand (collision + name validation, DisableFlagParsing), dispatchPluginCommand (G2 hash re-verification + bounded dispatch + exit-code propagation), pluginDispatchEnv whitelist, checksum sidecar helpers (writePluginChecksum/readPluginChecksum/removePluginChecksum), pluginRegWarn/flushPluginRegWarnings.","cmd/plugin_registration_test.go (new): 15 hermetic tests covering registration in eka --help + landing under the Plugins group, no-commands → no group, dispatch args/env/exit-code contract, dispatch-time anti-TOCTOU refusal, registration-time tamper skip, PATH-only refusal, PATH shadow warning, built-in collision, cross-plugin collision (deterministic first-wins), probe timeout \u003c= 2s, broken plugin skip, unverifiable (no sidecar) skip, invalid command name refusal, cache TTL + reinstall invalidation, per-process dispatch memoize.","cmd/plugin.go: install finalize writes the G2 checksum sidecar (refusal on hash/record failure, install rolled back).","cmd/plugin_lifecycle.go: update finalize replaces the sidecar with the new binary's checksum; remove deletes the sidecar.","cmd/help_groups.go: dynamic Plugins group (groupPlugins), commandGroupsFor/hasPluginCommands shared by root help and landing.","cmd/root.go: registerPluginCommands at root construction (dynamic group added only when plugin commands exist), flushPluginRegWarnings after streams are set, landing renders the current tree via commandGroupsFor."],"role":"implementation","summary":"B1 deferred plugin command registration (ADR-031) implemented in eka-cli: cmd/plugin_registration.go registers commands declared in the manifest of installed OFFICIAL plugins into the cobra tree at root construction, grouped under the dynamic Plugins group (cobra GroupID per sto:help-ux renderCommandGroups); dispatch runs the plugin executable under the bounded env whitelist with the plugin's exit code propagated. G-series trust boundary: G1 official-registry-only; G2 anti-TOCTOU — install/update finalize writes the binary SHA-256 sidecar (\u003cdir\u003e/.eka-\u003cname\u003e.sha256) and every dispatch recomputes the hash against it; G3 plugin dir over PATH — PATH-only plugins visibly refused, PATH shadows warned; G4 collision refusal (built-ins/other plugins, deterministic first-wins) + per-process manifest cache with TTL, invalidated on reinstall (binary file identity). Failure semantics never degrade the CLI: probe bounded at pluginProbeTimeout \u003c= 2s, broken/hung/unverifiable/colliding plugins skipped with visible deterministic warnings. Dynamic grouping implemented as a runtime mapping in the CLI registration path (plan:cli-experience is immutable; documented in ADR-031 Decision 9).","tests":["go test -race -count=1 ./... — 686 passed, 0 failed (baseline 671; +15 new registration tests).","gofmt -l . — clean; go vet ./... — clean; go build ./... — OK."]}