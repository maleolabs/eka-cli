{"description":"Define the explicit rule for delegation-mode conflicts on resume in both command bodies: what happens when the re-resolved mode (per the Delegation mode section's re-check-on-resume step) differs from the mode recorded in the checkpoint. Amendment to the shipped solo-mode degrade path (R8).","acceptanceCriteria":"1. The canonical `## Delegation mode` section gains an explicit conflict rule, byte-identical in eka-discuss.md and eka-execute.md: on resume, if re-resolved mode differs from the checkpoint-recorded mode, the agent states the conflict explicitly, reconciles per a defined precedence (recorded checkpoint mode governs already-completed items; newly started items follow the freshly resolved mode), and records the decision — never silent.\n2. The rule preserves store authority and runtime gates (mode never overrides EKA state machine obligations).\n3. Wave invariants intact: zero alex-/althea- tokens, description-only frontmatter, primitive tables untouched; Skill loading sections untouched except where the amendment strictly requires.\n4. No ecosystem names hardcoded (no codex/claude/opencode strings in bodies).","debtRationale":"Review of ts:solo-mode-degrade flagged the missing conflict rule as a minor gap: mid-run edits to DELEGATION.txt (or mapping changes between runs) can flip the resolved mode while a checkpoint records the old one. Impact is low today because the store stays authoritative, but the honest-degrade contract (never silent) is incomplete without an explicit reconciliation rule."}