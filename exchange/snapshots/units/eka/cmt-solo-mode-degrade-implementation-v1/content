{"changes":["skills/commands/eka-discuss.md: canonical '## Delegation mode' section after Role contract; solo-perspective bullets in Gate 2 (deep analysis) and Gate 4 (review); Output records delegation mode","skills/commands/eka-execute.md: same canonical '## Delegation mode' section; intro scoped outside mode: solo; Phase 1 resolves mode before plan execution; Phase 2 step 3 solo inline implementation under identical worktree isolation; step 5 self-review semantics with recorded 'self-review (mode: solo)' notes; checkpoint template gains 'mode: solo | delegated'; Phase 5 reports mode + self-review records; Hard rules: team-review rule notes solo self-review satisfies it, primary-implements prohibition scoped to non-solo mode","skills/commands/README.md: Provider mapping paragraph documents Delegation mode resolution, recording, and both modes' semantics briefly"],"role":"implementation","summary":"Implemented the explicit solo-mode degrade path (R8 of req:agent-agnostic-skill-pack) on feat/ts-solo-mode-degrade @ 7ed2dcb. Added a canonical 'Delegation mode' section, byte-identical in both command bodies, placed after Role contract: the primary resolves delegation rows from the active delegation data (DELEGATION.txt sidecar when present, else the pack's mapping table) BEFORE any delegation attempt; every role resolving to solo/primary =\u003e mode: solo, any named agent =\u003e mode: delegated; the resolved mode is recorded in the session preamble, checkpoints and closing summaries, never silently (missing/unreadable delegation source defaults to mode: solo with the assumption stated). Bodies stay ecosystem-agnostic: detection is from delegation data only, zero ecosystem names in the new sections. /eka-discuss degrades benignly: solo analysis roles (architect, code-review, security-review, product-review) are performed inline as clearly labeled perspectives against the same inputs/deliverables; gate structure and outputs preserved; closing summary records the mode. /eka-execute solo semantics: primary performs implementing roles inline under the SAME isolation rules as delegated work (dedicated branch + worktree per item from latest develop snapshot, quality gates re-run, merge before done); team review becomes explicit self-review against the same checklist when no second agent exists, recorded as cmt notes (--role review) whose summary states 'self-review (mode: solo)' - a rubber-stamp pass without the checklist violates the gate; orchestration/state-machine duties unchanged; checkpoint template gains a 'mode:' line; Hard rules amended so the primary-implements prohibition is scoped to non-solo mode. Delegated-mode behavior unchanged. mappings/codex.toml needed no change: its all-solo rows (mode=\"solo\" agent=\"primary\", deliberate pack policy - NOT a Codex capability claim) already match what the bodies now say.","tests":["gates: GOCACHE=/tmp/opencode-marij/go-cache GOTMPDIR=/tmp/opencode-marij go build ./... \u0026\u0026 go vet ./... \u0026\u0026 go test ./... =\u003e ok github.com/maleolabs/eka-mcp, ok .../cmd/eka-mcp, ok .../internal/eka, ok .../internal/mcp (all pass)","invariant grep: grep -n 'alex-|althea-' over skills/commands/*.md =\u003e CLEAN (zero legacy agent tokens)","invariant grep: grep -in 'codex|claude|opencode' over extracted Delegation mode sections AND full bodies =\u003e CLEAN (no ecosystem names hardcoded)","byte-identity: sha256 of '## Delegation mode' section identical in both bodies (3f31e8f3f9017c0622af69827693db9230113fd321f6a4e2ac2b97c4b4ba354b)","byte-identity vs HEAD 3c9dfdb: Transport primitives sections unchanged in both bodies (discuss aa86dea0..., execute 6647b683...)","byte-identity vs HEAD 3c9dfdb: Skill loading sections unchanged in both bodies (9af9eede57b5f86d794d1ae30dc5382606e6888f060600702365b163ab6dceb3, identical across bodies)","frontmatter check: exactly one 'description:' key inside frontmatter of each body (description-only preserved)"]}