{"description":"Implement the hardcoded official plugin registry (eka-cli/plugin/registry.go: name -\u003e repo) and the `eka plugin install` command: resolve name, download release asset, verify SHA-256 against SHA256SUMS.txt, install to ~/.eka/plugins, smoke-check manifest.","acceptanceCriteria":"`eka plugin install mcp` installs the official plugin and it becomes discoverable by `eka install skills/commands`. Corrupt checksum refuses (fail-closed)."}