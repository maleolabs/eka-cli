{"changes":["MCP write tools assign/reassign/unassign (schema eka-assignment-v1) mirroring CLI semantics exactly via Authoring API/store — same target/member forms, same validation (work-item gate, namespace ownership, member resolvability with repo-scoped list, cross-repo refusal, legacy draft refusal, CKO validation), same refusal classes, no partial writes (assign via Relate no-churn, reassign/unassign via RepointUnit + draft mutation + ValidateDraft)","Capability layer internal/eka/assignment.go (847 lines) implementing single-assignee ADR-029 semantics, deterministic agent identity (AuthorIdentity{Kind:\"agent\", Name:\"mcp-agent\"} default via resolveAuthor, byKind validation), and store/draft mutation paths","Server layer internal/mcp/server.go: extended Capability interface, AssignmentRequest/UnassignRequest types, 3 tool definitions in tools/list (assign/reassign/unassign) with pinned schemas, and 3 dispatch cases with by/byKind resolution","Conformance suite extended: tools/list exact 18-tool order, inputSchema required fields, assignment description checks, byte-determinism for each tool, missing-arg and unknown byKind refusals (isError), capability-level refusal sanitization; server unit tests for assign/reassign/unassign including agent identity default and malformed-arg fuzz cases","Capability integration tests (internal/eka/capability_test.go): assignmentEnv via Authoring API (NewDraft+Publish) with proper structured JSON payloads, covering assign no-churn, idempotent same-target, different-target refusal, reassign single-operation, unassign removal/no-op, member resolvability, non-work-item gate","Docs: README.md tool table 15→18, skills/docs/mcp-boundary.md new Assignment section with §8 decision-3 cross-reference, CHANGELOG.md Unreleased assignment entry","Hardening: error sanitization (no paths/stack traces), bounded line handling, batch rejection unchanged; fuzz corpus for assignment malformed args (null/empty/wrong-type/array) asserts no crash/hang"],"role":"implementation","summary":"Implements MCP assignment write tools assign/reassign/unassign per td:mcp-assignment-tools AC 1-6: exact CLI semantics, explicit agent author identity, deterministic refusals, no partial writes, conformance + fuzz, docs, gates green.","tests":["go build ./... — ok","go vet ./... — ok","go test ./... — ok (internal/eka 35 tests including 8 assignment integration tests, internal/mcp conformance + server tests including assign/reassign/unassign byte-determinism and refusal cases, fuzz corpus 10k+ malformed messages 0 crash)","Manual fixture: seeded repo acme with mbr:alice/bob + sto:item-a, verified assign → published, reassign → published, unassign → published, idempotent and refusal paths via capability layer"]}