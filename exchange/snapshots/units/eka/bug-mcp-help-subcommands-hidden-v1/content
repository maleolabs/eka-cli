{"description":"`eka mcp -h` renders only the generic proxy stub and hides the plugin's actual subcommands (manifest, install, configure, serve). Operators and agents cannot discover the MCP tooling surface from the CLI alone. Fix disclosure so the help lists every subcommand the installed plugin exposes, per the plugin command registration contract (req:eka-mcp-production locked decision #2/B1; rich descriptions were deferred as B3 — this item is about DISCLOSURE, not rich descriptions).","impact":"Discoverability defect observed 2026-08-23: the four plugin subcommands exist and work, but `eka mcp -h` discloses none of them; agents and operators must already know the surface to find it. Root cause spans both sides — eka-mcp manifest declares no commands section, and eka-cli's native stub help embeds no static list."}