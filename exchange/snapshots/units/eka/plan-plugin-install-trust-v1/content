{"objective":"Implementation plan for the plugin install + trust feature.","scope":"1. Registry + Source type (eka-cli/plugin/registry.go). 2. `eka plugin install` with download + checksum + smoke-check. 3. `eka plugin list/remove/update`. 4. Two-tier trust + consent. 5. eka-mcp `manifest --json` capabilities + source. 6. eka-mcp release pipeline (binaries + SHA256SUMS).","outOfScope":"Binary signing, sandboxing, remote registry, run-time enforcement (see scope)."}