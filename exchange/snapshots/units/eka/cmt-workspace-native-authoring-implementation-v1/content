{"changes":["cli: eka new --project/--namespace (cmd/authoring.go) — explicit workspace-native scope; the pair scaffolds into EKA_HOME/drafts/\u003cproject\u003e/ for a project REGISTERED in the workspace (run 'eka project list') from ANY directory, inside or outside a repository","cli: resolution rules (resolveNewScope + new resolveExplicitScope) — explicit path never borrows project/namespace from eka.yaml (ADR-017 D6 rejected --project as a repo override; the explicit pair is the cross-project slot D6 kept open); bare --project requires --namespace or a qualified target; --namespace alone resolves the project from the repository context; namespace must be a valid EKA identifier (metadata.ValidIdent)","cli: registry gate — an explicit --project must name a registered project (r.Workspace.Projects()), else a deterministic refusal with the 'eka project list' / 'eka sync' / 'eka project register' hint","cli: repo-context path unchanged — no flags keeps the ADR-018 gate, the repo project/namespace defaults and the D6 cross-namespace refusal; publish/edit/discard stay ADR-018-gated (documented boundary: publish resolves workspace-native drafts through the cross-project fallback from inside a registered repo)","cli: batch form (cmd/authoring_batch.go) shares the explicit resolution — eka new --file \u003cbatch\u003e --project/--namespace scaffolds the whole set outside a repo; a batch has no target namespace, so --project always requires --namespace","core: no production change required — Authoring.NewDraft already accepts an explicit Project/Namespace and is repository-free; only the CLI resolution gated it; the workspace registry (runtime.Workspace.Projects) already exposes the registered-project list"],"role":"implementation","summary":"Workspace-native authoring: eka new --project/--namespace targets a registered project from any directory","tests":["cli: TestNewWorkspaceNativeOutsideRepo — acceptance (explicit pair creates the draft outside any repo), qualified-target namespace, unregistered-project refusal, bare --project refusal, --namespace alone refusal, invalid-identifier refusal, ADR-018 gate unchanged","cli: TestNewWorkspaceNativeInsideRepoOverride — explicit pair overrides the repo identity deterministically, deliberate foreign namespace allowed on the explicit path, --namespace alone uses the repo project, D6 unchanged on the implicit path, own-project still requires --namespace","cli: TestNewBatchWorkspaceNativeOutsideRepo — batch set scaffolded under the explicit project, --project without --namespace refused, --namespace alone refused, ADR-018 gate unchanged","cli: TestPublishUnknownFlags (was TestNewPublishUnknownFlags) — publish keeps --project unknown (D6); eka new re-gained the pair for workspace-native authoring","core: TestNewDraftWorkspaceNativeExplicitScope — Authoring API scaffolds an explicit project/namespace with no eka.yaml and no registered repository; draft listed under the explicit project","gates: gofmt -l . empty and go vet ./... clean in both worktrees; go test ./... — cli 549 passed, core 672 passed","smoke (default workspace ~/.eka): from /tmp/opencode/ws-scratch (no eka.yaml) — eka new dec:ws-smoke-1 --project eka --namespace eka exit 0, draft at ~/.eka/drafts/eka/; eka new without target refused (ADR-018 gate, exit 1); eka draft validate resolves the workspace-native draft cross-project (exit 0 on a publishable sto draft); eka publish outside a repo refused (exit 1) and from the eka-cli worktree published eka/sto:ws-lifecycle:1 via the cross-project fallback; inside the worktree eka new sto:in-repo-1 unchanged (project eka, ns eka) and the D6 foreign-namespace refusal intact"]}