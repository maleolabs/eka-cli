{"objective":"Implementation plan for phase 2 (joint review/approval): additive projection + renderer + docs — no engine changes.","scope":"Ordered: 1. sto:phase2-core-view — eka-core view grouping + summary (foundation for CLI). 2. sto:phase2-cli-render — eka-cli renderer + help. 3. sto:phase2-mcp — eka-mcp read-only tool + skill pack docs (can run in parallel with CLI after core view lands). Container planned; activation after the units ahead in the roadmap (plugin-install-trust active; cli-experience, eka-mcp-production, release-coordination planned; phase 2 follows phase 1 team-collaboration container).","outOfScope":"Same as scp:team-collaboration-phase2 (isolation deferred, no rule/gate changes, no dismiss command, no mbr- linking, no standard bump, no preemption)."}