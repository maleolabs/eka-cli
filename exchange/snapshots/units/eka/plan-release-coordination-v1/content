{"objective":"Implementation plan for the release-coordination scope: pipeline first (unblocked), then order documentation, then the two gated items (standard 1.1 bump and the eka-core release with the plugin package).","scope":"Ordered by dependency and gating: 1. sto:release-core-pipeline — immediate (no upstream dependency). 2. sto:release-order-docs — immediate; documents the order the other two items follow. 3. sto:standard-111-bump — gated on team-collaboration's standard changes (sto:assigned-work-standard + sto:assigned-work-core landing in the same eka-core release); delivers the coordinated standard 1.1 change set. 4. sto:core-release-with-plugin — gated on sto:mcp-contract-relocation (eka-mcp-production container); cuts the eka-core release with eka-core/plugin, then verifies eka-cli and eka-mcp build against it via explicit go.mod bumps. The container is planned; activation order is the executor's decision honoring the gates.","outOfScope":"Same as scp:release-coordination (library signing N/A, no orchestration service, no cross-repo automation, requiresEka CLI-axis, FEEDBACK_ISSUE_TOKEN deferred, no preemption)."}