{"description":"eka get --downstream never returns a downstream array, even for qualified relationship targets. --upstream works. --downstream silently returns nothing (no field, exit 0). Repro: workspace ~/.eka schema v4 eka v1.2.3: five adr- units carry qualified derives-from -\u003e walkie/arc:system-overview, eka get walkie/arc:system-overview --downstream returns no downstream key. Same for nest-desacorp-web/cmt:activity-reporting-review discusses -\u003e req:activity-reporting, and walkie/vis:terminal-mesh-comms referenced by 7 units. All qualified edges, upstream traverses correctly, downstream absent. Verified in 3 projects, 3 relationship types, versioned and line forms. This is NOT the unqualified-target bug (#15/#22).","impact":"Reverse-edge discovery unavailable: 'what references this artifact?' cannot be answered. Core immutability workflow requires blast-radius before amending/superseding. eka-knowledge-review prescribes eka get --upstream --downstream; downstream now false-negatives as 'nothing depends on this' licensing unsafe change. Blocking for architecture traceability."}