{"description":"Deliver the per-agent configuration UX: eka-mcp configure subcommand (outside the fixed manifest/install contract) that writes the MCP client config and delegates skill/command install, with --dry-run safety, unsupported-target refusal, manual documentation, and graceful behavior without a workspace.","acceptanceCriteria":"eka-mcp configure [--target opencode|claude|codex] [--dir] [--dry-run] --json works: writes the MCP client config entry (opencode.json mcp section / ~/.claude.json mcpServers / ~/.codex), absolute binary path + EKA_HOME, then delegates install skills/commands; unsupported --target fails with deterministic error naming supported targets; never overwrites other servers' entries; --dry-run prints plan without writing; README has manual config snippets per ecosystem; server starts and answers cleanly with no workspace present (EKA_HOME unset), reporting uninitialized state deterministically."}