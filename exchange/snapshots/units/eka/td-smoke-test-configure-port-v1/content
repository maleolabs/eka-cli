{"description":"Port skills/scripts/smoke-test.sh section 8 from the removed `eka install` surface to the current `eka-mcp configure --dry-run --json` surface (ADR-030 reality). The script is the pack's documented validation path (referenced by installation.md and manifest verifyPack) and is currently broken against develop.","acceptanceCriteria":"1. smoke-test.sh section 8 no longer invokes `eka install` anywhere in the script (grep clean).\n2. Assertions drive `eka-mcp configure --target opencode --with-all --dry-run --json` (and at least one non-dry-run install into a temporary fake HOME) validating: exit codes, valid JSON output, changes[] actions, counts {created|overwritten|skipped}, DELEGATION.txt sidecar presence and non-.md extension, idempotent second run reporting skips.\n3. Codex refusal case asserted: configure --target codex --with-commands exits non-zero deterministically with no writes.\n4. Script runs green end-to-end against a built eka-mcp binary; documented usage in installation.md still matches the script's behavior.\n5. No pack content files (skills/**, mappings/**) modified by this item.","debtRationale":"The validation path silently broke when ADR-030 removed `eka install`: the script still exercises a command that no longer exists, so the pack's own smoke gate cannot pass. Discovered during td:packaging-cleanup-sync review (2026-08-23); pre-existing file outside that item's diff, therefore tracked separately."}