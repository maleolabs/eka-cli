{"changes":["eka-mcp cmd/eka-mcp/configure.go: add --with-skills/--with-commands/--with-all flags; runConfigure installs only opted-in families","dry-run reflects the same plan without touching the filesystem; Installed omitted from JSON when nothing opted in","BREAKING (intentional): callers relying on auto-install must pass --with-all; documented in README + CHANGELOG","rationale: skills/templates are already exposed as MCP resources (eka://skills/*, eka://templates/*), filesystem copies are a legacy duplicate"],"role":"implementation","summary":"Decouple eka-mcp configure from skills/commands installation: default writes only the MCP client config entry; installing artifacts becomes opt-in via flags.","tests":["cmd/eka-mcp/configure_test.go: 11 tests covering default-no-install, per-flag opt-in, dry-run plans, all three targets, merge preservation, JSON stability","manual E2E: default configure writes only opencode.json; --with-all installs 11 skills + 2 commands"]}