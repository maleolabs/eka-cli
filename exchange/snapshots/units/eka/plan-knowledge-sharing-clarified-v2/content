{"objective":"Knowledge sharing clarified per follow-up: per-project sharing (project name + version) with L0-L2 reference docs, EKA vs non-EKA detection via eka.yaml, deep audit adjusted to target level, dedicated shr export/import, interactive eka-share flow, proper namespace handling, plus delete shared knowledge from workspace.","scope":"1) EKA vs non-EKA detection (check eka.yaml exists; EKA uses eka.yaml project/namespace, non-EKA asks user for project id, no hardcode eka). 2) Per-project identifier: project id from eka.yaml (EKA) or user input (non-EKA) + version with semver immutability (3-part: major immutable, minor/patch override; 2-part: minor immutable, patch override). 3) Level trusted to user (EKA provides tool, not restriction). 4) List/filter/get specific/use as reference (eka get operations --type shr --level --project --version, alias records). 5) New parent commands eka shr export/import for ekapkg (RSF) with type distinction live KMS vs shared knowledge (do not reuse existing export/import). 6) Deep audit for non-EKA adjusted to target level (L0 shallow OK, L1/L2 deep scan docs + codegraph + sensitivity redaction). 7) Namespace derived from target project, not source repo. 8) Skills split: eka-shr-builder (EKA) and eka-shr-non-eka (non-EKA deep audit) both English, eka- prefix, plus general command eka-share with interactive Q\u0026A (level, identifier, provenance, title, export choice). 9) Delete shared knowledge from workspace (eka shr delete/remove with confirmation, e.g. eka shr delete eka/shr:\u003cid\u003e or eka shr delete --project \u003cname\u003e --version \u003cv\u003e --level L0) to remove mistaken shares without disturbing other references; respects immutability (major/minor) but allows override/delete for correction.","outOfScope":"Daemon live sync, semantic ranking/search, signing/verify for ekapkg, and full non-EKA codebase migration to EKA (only spike audit for sharing). Existing 5 stratum and 29 tokens unchanged."}