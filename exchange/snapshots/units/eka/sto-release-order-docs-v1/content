{"description":"Document and operationalize the release order: standard -\u003e core -\u003e cli -\u003e mcp, consistent with req:prd dependency direction, with validation steps so a consumer release never silently builds against a stale dependency.","acceptanceCriteria":"The one-way release order (eka-standard -\u003e eka-core -\u003e eka-cli -\u003e eka-mcp) is documented (in eka-core release docs and referenced from this requirement); each consumer release work item includes an upstream-release-exists validation step (e.g. gh release view or imported-constant/asset check) and an explicit go.mod bump (go get) step; Go module rules documented (no tag delete/recreate, -rc for trials, no major bump without /v2 path)."}