{"objective":"MVP scope for the cli-experience requirement: two work items delivered in one execution container.","scope":"1. sto:init-standard-declaration — `eka init` writes the EKA standard declaration file into the generated repository; file vendored from the eka-standard GitHub release asset (v1.0) and embedded in the CLI binary; ADR-020 amendment; standardVersion derived from the embedded file; build-time version consistency test. 2. sto:help-ux — root help and landing command list grouped by intent (Authoring / Repository \u0026 Exchange / Knowledge Access / Runtime \u0026 Workspace / Utility) and colored TTY-only (commands Info, flags Dim, headers Accent, hint Dim); subcommand help colored, flat; assertNoANSI coverage extended to help paths.","outOfScope":"Network fetch at init (no --offline, no --standard-version flag); SHA256SUMS verification for the standard asset (eka-standard publishes none; the file is vendored and embedded instead); the remaining `eka version --json` multi-axis surface of sto:version-clarity; grouping in subcommand lists; eka.yaml schema changes."}