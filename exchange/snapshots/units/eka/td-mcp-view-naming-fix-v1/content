{"description":"Fix the MCP tool naming collision found during ctr:agent-agnostic-skill-pack execution: the MCP `view` tool reads draft files verbatim (authoring aid), while CLI `eka view` projects the human-oriented knowledge model. Rename the MCP tool to `draft_read` and ship the old name as a deprecated alias for one minor version before removal (req:eka-mcp-production revision 3 §8.2 decision 2).","acceptanceCriteria":"1. New tool `draft_read` provides the exact current `view` capability (draft file content verbatim, deterministic).\n2. Old name `view` remains functional for one minor version, flagged deprecated in its tools/list description with a migration note naming `draft_read`.\n3. Removal of the alias tracked (code comment + changelog entry referencing this item); conformance tests cover both names during the window.\n4. Docs updated wherever the draft-reader tool is referenced (mcp-boundary.md, README tool table); explicit statement that human projections remain CLI-only (`eka view`/`eka watch`).\n5. Full gates green in eka-mcp: go build ./..., go vet ./..., go test ./...","debtRationale":"Agents assuming name equivalence between MCP `view` and CLI `view` misread capabilities — documented during wave-1 implementation review of ts:role-contract-bodies (2026-08-23), where the primitive tables deliberately carried `—` for the projection row because mapping them would be wrong."}