{"changes":["skills/commands/eka-discuss.md: added step 4 'Reconcile on resume.' to the '## Delegation mode' resolution protocol (+1 line, identical text to eka-execute.md)","skills/commands/eka-execute.md: added step 4 'Reconcile on resume.' to the '## Delegation mode' resolution protocol (+1 line, identical text to eka-discuss.md)","Byte-identity proof: post-edit extraction of the full '## Delegation mode' section from both bodies hashes identical (md5 408592fd0dd57b38cf9716f8f72697de on both); pre-edit sections were also verified byte-identical before amending","Wave invariants verified post-edit: zero alex-/althea-/codex/claude/opencode tokens in both bodies; frontmatter untouched (description-only); transport-primitive tables and Skill loading sections untouched; git diff shows exactly one inserted line per file"],"role":"implementation","summary":"Implemented eka/td:solo-mode-resume-conflict: the canonical '## Delegation mode' section in both command bodies (skills/commands/eka-discuss.md, skills/commands/eka-execute.md) gained an explicit resume-conflict rule as step 4 ('Reconcile on resume'). On resume, if the re-resolved mode differs from the checkpoint-recorded mode, the agent states the conflict explicitly — never silently — and reconciles by fixed precedence: the recorded checkpoint mode governs already-completed items (history stays coherent with the mode that produced it), while newly started items follow the freshly resolved mode. The reconciliation decision is recorded in the resumed session preamble and carried into the next checkpoint. The rule explicitly preserves store authority and runtime gates: EKA state-machine obligations (transitions, gates, evidence notes) apply identically in either mode and always win over the mode.","tests":["go build ./... — OK","go vet ./... — OK","go test ./... — all packages ok (eka-mcp, cmd/eka-mcp, internal/eka, internal/mcp)","grep -E 'alex-|althea-|codex|claude|opencode' over both bodies — no matches","md5 comparison of extracted '## Delegation mode' sections across both bodies — identical hash post-edit"]}