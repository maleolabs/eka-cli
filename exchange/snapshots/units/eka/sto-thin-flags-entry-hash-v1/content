{"description":"Additive EKA filters: Entry.objectHash (add to eka-context-v1 Entry), --no-history and --only-section=constraints,dependencies,decisions,planning,review (alias --level 0..6 sugar later), --max-units/--max-bytes with truncated:true envelope. Deterministic slicing by sorted canonicalForm first-role-wins, bounded 64 awareness.","acceptanceCriteria":"eka context --no-history omits history, --only-section constraints returns only constraints, --max-units N returns first N sorted entries + summary.truncated:true + pagination. Entry carries objectHash. All outputs byte-identical for same (subject,depth,optsHash) across two runs. Existing eka context/get golden tests still pass. No token counting inside EKA."}