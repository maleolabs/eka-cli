{"changes":["internal/mcp/conformance_test.go (new, 14 tests): asserts the spike contract — initialize echoes client versions (2024-11-05/2025-03-26/2025-06-18/2025-11-25), baseline 2024-11-05 fallback when none announced, capabilities tools+resources only, tools/list exact get/domain/status with inputSchema, -32003/-32002, notifications silent, ping {}, error codes -32700/-32600/-32601/-32602, stdio newline framing + flush per response.","internal/mcp/server.go: batch arrays rejected deterministically (-32600 fixed message, id null, never dispatched to the capability layer — proven by call-recording test); parse errors use a fixed message (no Go parser internals); sanitizeError (first line + path redaction) applied to all tool/resource error paths; validID rejects object/array/bool ids; non-object messages refused; errorResponse sets id null when id detection failed (JSON-RPC 2.0 §4.3).","internal/mcp/transport.go: 64 MiB read-line cap enforced fragment-based BEFORE unbounded allocation, with drain + resync at the next newline; refusal is deterministic and the session continues; cap parameterized (Server.maxLineSize) so boundary tests run at a small cap while one real-cap test remains; ~2x transient capacity documented.","internal/mcp/fuzz_test.go: FuzzHandleMessage + FuzzServe Go fuzz targets (real entry points) + TestFuzzCorpus (10k deterministic messages, seed 42, recover-guard + 30s timeout) running in every go test.","scripts/integration-opencode.sh (new): builds the binary, registers it as a stdio MCP server in a throwaway opencode project, runs a real session, asserts the status tool is called and JSON is received; requirements documented + guarded (opencode/python3/timeout/go); robust STATUS_RESULT extraction.","cmd/eka-mcp/main.go: startup errors sanitized via mcp.SanitizeError (no workspace path leak to stderr).","Review fixes (224ce11): pathRe broadened (spaces, tilde) + relPathRe for dot-less relative paths with 3 new no-leak test cases; id null on malformed-object paths; script extraction robustness + dep guards; codeResourceFound renamed to codeResourceNotFound."],"role":"implementation","summary":"Harden the MCP server to production standard: protocol conformance suite (echo + fallback per spike conclusion), deterministic batch rejection, refusal-class error message policy with no internal leakage, 64 MiB bounded read line, fuzz robustness (10k+ corpus, 0 crash/hang), and a real-opencode integration test. 97 tests pass, race clean.","tests":["go test -count=1 ./... — 97 passed, 0 failed (root 7, internal/eka 8, internal/mcp 47, cmd/eka-mcp 6); existing 17 server tests untouched and passing.","go test -race -count=1 ./internal/mcp/ — 76 passed, 0 failed.","Fuzz: FuzzHandleMessage 70k+ execs 0 fail; FuzzServe 47k+ execs 0 fail; TestFuzzCorpus 10k messages 0 crash 0 hang.","scripts/integration-opencode.sh — PASS against real opencode 1.18.19 (status tool → valid JSON, SchemaVersion 4).","gofmt -l . empty; go vet ./... clean; go build ./... OK."]}