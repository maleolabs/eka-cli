{"description":"Add the tag-driven release pipeline to eka-core (the only ecosystem repo without one): GitHub release with notes, adapted for a library (no binaries/checksums — integrity via go.sum + sumdb), with semver, duplicate-tag, and quality gates.","acceptanceCriteria":"eka-core/.github/workflows/release.yml exists: triggered by vMAJOR.MINOR.PATCH tag (rc suffix allowed), semver regex check, duplicate-release check (gh release view fails fast), quality gate (gofmt/vet/test) runs inside the workflow before release creation, GitHub release with --generate-notes; release process documented in eka-core README/CONTRIBUTING; pushed tags never deleted/recreated (documented)."}