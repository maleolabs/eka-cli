{"changes":[],"role":"implementation","summary":"Planning discussion 2026-08-22 (session 2) on eka/fnd:agent-agnostic-skill-pack — resolving open item #4 (frontmatter render ownership) at requirement time as the fnd mandates. Sub-agent analysis: alex-architect (mechanism ownership — plugin contract v1 in cmd/plugin.go, cmd/plugin_lifecycle.go, cmd/plugin_registration.go; knowledge lineage ADR-022/023/030 + req:eka-mcp-production + scp:eka-mcp-production v2). KEY FINDING: ADR-023 (eka install command) is OBSOLETE — removed (commit 6600f38) and superseded by ADR-030 (MCP contract relocation to plugin executable); fnd references to ADR-023 installer semantics are historically stale and the requirement must cite ADR-030 lineage. Verified state: eka-cli has NO top-level install surface (cmd/root.go registers five static intent groups + dynamic Plugins group; bootstrap/ is init scaffolding only); eka plugin install distributes binaries into ~/.eka/plugins only, never touches agent configs or the canonical store; artifact installation (manifest --json / install \u003ckind\u003e --dir) is executed BY the plugin executable; skill pack is embedded in the eka-mcp binary (review finding F1); target-aware UX already mandated by sto:mcp-configure-ux (eka-mcp configure --target opencode|claude|codex with --dry-run). DECISION DIRECTION (pending user confirmation): rendering/installation is PLUGIN-OWNED — co-located with the embedded pack, mappings/, and provider-frontmatter templates in eka-mcp's install/configure surface; eka-cli exposes no parallel install surface. Binding statements proposed for req: (1) command bodies carry description-only frontmatter, provider frontmatter rendered at install time never stored canonically; (2) pack ships pre-rendered role-to-agent mapping tables per ecosystem inside the pack; (3) installer does pure-copy of bodies + render of provider frontmatter + copies active mapping as non-.md sidecar (DELEGATION.txt pattern); (4) rendering owned by pack-distribution vehicle (today eka-mcp), eka-cli exposes none; (5) dry-run support + deterministic refusal of unsupported targets. Deferred to implementation: empirical sidecar scan verification (Claude Code/opencode), exact sidecar filename/location per ecosystem, overwrite/reinstall semantics, B1 help-surface registration. Risks acknowledged: ownership defined relative to pack vehicle (moves if pack moves), users without eka-mcp cannot install skills (documented), mapping-vs-body drift needs build-time consistency check.","tests":[]}