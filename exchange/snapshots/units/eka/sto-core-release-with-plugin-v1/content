{"description":"Cut the first eka-core release that contains the eka-core/plugin package (the A2 contract relocation), unblocking eka-mcp's dependency drop and the ecosystem's release cadence.","acceptanceCriteria":"GATED on sto:mcp-contract-relocation. After landing: eka-core released (tag) containing eka-core/plugin; eka-cli and eka-mcp go.mod bumped to the released version via explicit go get; both build against the release (no source pins); release notes cover the plugin package."}