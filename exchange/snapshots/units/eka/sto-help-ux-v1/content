{"description":"Root help (`Available Commands`) and the landing `Commands` list are grouped by intent — Authoring / Repository \u0026 Exchange / Knowledge Access / Runtime \u0026 Workspace / Utility — with cobra AddGroup/GroupID as the data model and a shared renderer (renderCommandGroups used by both help and landing). Colors TTY-only via the ui.Style paint path: command names Info (soft blue, the palette primary/heading hue), flag names Dim (gray, secondary tier), group headers Accent, help hint Dim, descriptions plain. Amber is not used for flags (semantic clash with warnings). Subcommand help gets colors only — group headers are noise for lists of ≤4 entries. assertNoANSI coverage is extended to help paths; unit tests assert group membership and color rendering with Style{Color:true}.","acceptanceCriteria":"Root help shows the five intent groups with correct command membership (init in Repository \u0026 Exchange, transition/note in Authoring, get/context/view/watch in Knowledge Access, sync/status/integrity/snapshot/project in Runtime \u0026 Workspace, update/version/feedback/completion/help in Utility). Landing `Commands` list uses the identical grouping. TTY: commands Info, flags Dim, headers Accent, hint Dim, descriptions plain. Non-TTY output is byte-identical to the current contract — no ANSI anywhere, assertNoANSI covers help paths. Subcommand help is colored but flat (no group headers). Existing help tests pass; new tests cover group membership and color rendering."}