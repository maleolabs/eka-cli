{"changes":["cmd/plugin.go: extracted the install runner's shared pieces — installTarget, resolveLatestRelease (tag + size + checksum from the tag-pinned SHA256SUMS.txt) and downloadVerified (staged download, fail-closed checksum verification, chmod) — so install and update share one verified-download path; PR #11 install behavior/messages preserved (all install tests green)","cmd/plugin_lifecycle.go: new — newPluginListCommand (+runPluginList, collectPluginListEntries, eka-plugin-list-v1 JSON schema, ui.Table rendering, broken manifests visible as unknown, exit 0 always), newPluginRemoveCommand (pluginRemoveRunner, removes binary + .old marker, refusal when not installed, exit 1), newPluginUpdateCommand (runUpdate/runUpdateAll, unknown name refuses with known list, not-installed refuses with install hint, atomic replace preserving old until smoke check passes with restore-on-failure, old -\u003e new version printed, --all updates only installed official plugins and exits 1 when any update failed, empty --all is exit 0)","cmd/root.go: help text extended to mention plugin lifecycle commands (registration untouched)","cmd/plugin_lifecycle_test.go: new — 17 hermetic tests covering list (discovered+installed+tier, broken manifest visible, empty, --json determinism), remove (installed + stale .old, not-installed refusal, windows .exe), update (single with old-\u003enew version, unknown name, not-installed, checksum mismatch keeps old + cleans partial download, broken new manifest restores old, --all updates official only and never touches third-party, --all empty, --all failure exits 1, windows .exe), command tree + usage classes"],"role":"implementation","summary":"Implemented the plugin lifecycle commands in eka-cli: `eka plugin list` (discovered + installed + trust tier, --json optional, exit 0 always), `eka plugin remove \u003cname\u003e` (deletes the installed binary plus stale .old markers, refuses when not installed), and `eka plugin update [name|--all]` (re-downloads the latest verified release reusing the install flow's download+checksum+smoke-check path, atomic replace with the update command's .old rename dance, prints old -\u003e new version, restores the old binary when the new manifest smoke check fails). Trust tier is labeled from the official registry only; no consent prompts (later sto:plugin-trust-model item).","tests":["go build ./... — pass","go vet ./... — pass","gofmt -l . — clean","go test ./... — 606 passed, 0 failed (589 existing + 17 new)","GOOS=windows GOARCH=amd64 go build ./cmd/eka — cross-compile pass","manual smoke: eka plugin list (table + --json), remove refusal exit codes"]}