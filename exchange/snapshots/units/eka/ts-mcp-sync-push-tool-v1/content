{"description":"Implement the push-only sync surface for the eka-mcp MCP server per req:eka-mcp-production revision 3 §8.2 decision 1: new tools sync_status (per-repository snapshot freshness: digest, last push/pull timestamps) and sync_push (refresh repository snapshots from the workspace store). sync pull / full re-seed is deliberately NOT exposed — it re-points line references to older instances (silent regression hazard) and stays an operator-supervised CLI operation.","acceptanceCriteria":"1. tools/list exposes sync_status and sync_push with deterministic machine contracts; outputs are byte-deterministic for identical store state.\n2. sync_push mirrors CLI `eka sync push` semantics exactly: same snapshot compilation, same digest computation, same refusal classes; a failed push writes nothing partially.\n3. No tool exposes pull/--from-docs re-seed behavior; attempting it via tool arguments refuses deterministically naming the CLI as the sanctioned path.\n4. Error messages follow the established refusal-class policy (no internal paths, no store details, no stack traces).\n5. Conformance suite extended: tools/list-call + call semantics for both tools; fuzz cases added (malformed args, unknown repository).\n6. Documentation updated (mcp-boundary.md or equivalent + req §8 matrix cross-reference).\n7. Full gates green in eka-mcp: go build ./..., go vet ./..., go test ./...","debtRationale":""}