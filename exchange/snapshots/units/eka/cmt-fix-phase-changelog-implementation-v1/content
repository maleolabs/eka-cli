{"changes":["eka-core runtime/draft.go: scaffold the phase change-log entry (domain phase, from \"-\" to the given value, by the author) when NewDraftRequest.Phase is set; change-log block no longer gated on owned domains only; NewDraft/draftDoc/draftJSON doc comments updated","eka-core runtime/draft_test.go: add TestNewDraftPhaseChangeLogEntry — scaffolds plan:x --phase mvp, asserts the phase field + change-log entry (from/to/by) plus the owned-domain entries, and asserts ValidateDraft passes (no R7 phase error)","eka-cli cmd/authoring.go: update `eka new` help text — the change-log now covers every owned domain and the phase context when given"],"role":"implementation","summary":"Fix eka new --phase: the scaffolded draft now carries a change-log entry for the phase domain (\"-\" -\u003e the given value, by the author), so `eka new plan:x --phase mvp` + `eka draft validate` passes rule 7. Root cause: the scaffold template (runtime.NewDraft/draftJSON in eka-core, which cmd/authoring.go delegates to) set the phase field but never appended a phase change-log entry, so R7 reported \"no change-log entry for domain phase\". Fix scaffolds the entry alongside the field, consistent with the owned-domain entries (same date, same author, from \"-\"). Acceptance smoke-tested end-to-end with the CLI built against the fixed core: plan:x --phase mvp --dimension planning validates clean (0 errors, 0 warnings).","tests":["TestNewDraftPhaseChangeLogEntry (eka-core runtime, new regression test)","go test ./... eka-core: 622 passed, 0 failed","go test ./... eka-cli: 511 passed, 0 failed","acceptance smoke: eka new plan:x --phase mvp --dimension planning; eka draft validate plan:x -\u003e valid, exit 0"]}