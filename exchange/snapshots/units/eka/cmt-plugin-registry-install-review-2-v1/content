{"notes":["FINDING (blocking for done): acceptanceCriteria merujuk `eka install skills/commands` — command tersebut SUDAH DIHAPUS (eka-cli commit 6600f38, 'remove obsolete skill/command install'). AI-agent layer pindah ke eka-mcp; install path yang benar adalah `eka plugin install mcp` lalu `eka-mcp install \u003ckind\u003e` (plugin contract v1).","Fix: amend acceptance criteria menjadi: '`eka plugin install mcp` installs the official plugin to ~/.eka/plugins and it becomes discoverable; `eka-mcp install skills|commands` delegates artifact-family install into the agent config directory. Corrupt checksum refuses (fail-closed).' — atau padanan yang disepakati executor.","Context: ditemukan saat planning audit 2026-08-15 (sesi eka-mcp-production). ADR-023 yang menjadi dasar `eka install` akan di-supersede oleh ADR-030 (sto:mcp-contract-relocation)."],"role":"review","verdict":"changes-requested"}