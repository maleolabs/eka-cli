{"description":"Expose work-item assignment operations as MCP write tools per req:eka-mcp-production revision 3 §8.2 decision 3: assign, reassign, unassign — mirroring the CLI commands' semantics through the Authoring API with mandatory agent author identity.","acceptanceCriteria":"1. Tools assign/reassign/unassign wrap exactly the CLI command semantics (same targets, same validation, same refusal classes); no semantic drift between transports.\n2. Every write sets an explicit agent author identity (AuthorIdentity{Kind:\"agent\"}) — never the Engineering placeholder (locked decision #7 of the parent requirement).\n3. Deterministic refusals for unknown identities, non-assignable targets, and gate-restricted states; no partial writes.\n4. Conformance suite extended for the three tools (list-call semantics + refusal cases); fuzz cases for malformed args.\n5. Docs updated (tool table + mcp-boundary.md).\n6. Full gates green in eka-mcp: go build ./..., go vet ./..., go test ./...","debtRationale":"Assignment was never assessed when the v2 surface was locked; agents coordinating multi-agent execution currently cannot reassign work without shelling out to the CLI, breaking the MCP-first loop for team-collaboration scenarios."}