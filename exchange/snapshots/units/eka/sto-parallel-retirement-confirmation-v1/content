{"description":"Scope the remaining singular-active assumptions: plan retirement gate, work-item active-container confirmation/registration checks, retire guards, and all user-facing help/protocol text.","acceptanceCriteria":"1. Plan retirement (immutable -\u003e superseded) gates only on active containers deriving from THAT plan (scope-aware), not on unrelated active containers. 2. Work-item active-container confirmation resolves against active containers in scope. 3. Retire guards for active containers keep working per-scope. 4. transition/cmd help text and protocol comments describe per-repo rule + dependency gate (no stale exactly-one-active claims)."}