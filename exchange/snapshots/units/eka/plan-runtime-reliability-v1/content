{"objective":"Plan execution for runtime reliability fixes, ordered by blast radius.","scope":"1. context-unqualified-refs — fix authoring gate (eka new normalize + publish validation) then read-path defaulting. 2. downstream-missing — fix get --downstream index. 3. execution-view-planned-hint — add queued summary to view execution. Each item gated by review + validate.","outOfScope":"Same as scope. No standard bump."}