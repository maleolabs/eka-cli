{"description":"Harden cmd/eka-mcp configure config-file writing: migrate writeConfigureFile to the same temp+rename writer used by install.go writeFileScoped, and replace the silent reset-on-invalid-JSON behavior with a deterministic refusal. Follow-up from the security review of ts:installer-render-sidecar (2026-08-23).","acceptanceCriteria":"1. writeConfigureFile writes via CreateTemp(0600) -\u003e chmod -\u003e rename into the destination name; symlinked destinations are refused (link left intact), matching writeFileScoped semantics.\n2. Invalid/unparseable existing config JSON no longer resets silently: configure refuses deterministically (non-zero exit, clear message) and leaves the existing file byte-untouched unless an explicit overwrite flag/behavior is documented.\n3. Behavior change documented in help text/README where configure writes config files.\n4. Table-driven tests cover: fresh write, overwrite of pack-owned file, symlink refusal, invalid-JSON refusal with untouched file.\n5. Full gates green: go build ./..., go vet ./..., go test ./...","debtRationale":"Security review of the installer wave found writeConfigureFile still uses direct os.WriteFile: it follows symlinks (unlike the scoped writer) and silently resets user config on invalid JSON — inconsistent guarantees inside one command surface. Low likelihood, high surprise; cheap to align now while installer semantics are fresh."}