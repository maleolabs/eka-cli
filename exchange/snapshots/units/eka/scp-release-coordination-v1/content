{"objective":"Release-coordination scope: eka-core release pipeline (the only missing one), documented one-way release order, and the coordinated standard 1.1 bump across standard repo / std CKO / eka-core / eka-cli — delivered in one execution container, gated on the units it unblocks.","scope":"Four work items in one container: 1. sto:release-core-pipeline — eka-core release.yml (tag-driven, semver check, duplicate-release check, quality gate inside workflow, GitHub release + notes) + release-process docs. 2. sto:release-order-docs — the one-way order (standard -\u003e core -\u003e cli -\u003e mcp) documented and referenced from release work items, with upstream-release-exists validation steps and explicit go.mod bumps in consumer releases. 3. sto:standard-111-bump (gated on team-collaboration standard changes) — eka-standard repo artifacts (EKA file, tokens/overview.md 27-\u003e28, tokens/mbr.md, CHANGELOG) + new std CKO eka-standard-1.1.0 (amends 1.0.0) + eka-core SpecificationVersion 1.1 + 27-\u003e28 conformance enforcement (coordinated with sto:assigned-work-core, same eka-core release) + version-consistency test extension. 4. sto:core-release-with-plugin (gated on sto:mcp-contract-relocation) — cut the eka-core release containing eka-core/plugin; eka-cli and eka-mcp build against it via explicit go.mod bumps.","outOfScope":"Binary signing/checksum for eka-core (library); changelog automation beyond GitHub notes; monorepo/release-orchestration service; cross-repo release chaining automation (order is policy with manual validation); requiresEka refresh as part of the standard bump (CLI axis); FEEDBACK_ISSUE_TOKEN hardening; changes to existing eka-standard/eka-cli/eka-mcp release workflows; preemption of plugin-install-trust (active) or cli-experience (planned)."}