{"changes":["skills/manifest.yaml: installCommand -\u003e 'eka-mcp configure --target opencode|claude|codex [--with-skills|--with-commands|--with-all] --json (pack-distribution vehicle per ADR-030)'","skills/docs/installation.md: sections 2 and 5 rewritten from removed 'eka install' to 'eka-mcp configure'; 'ten' -\u003e 'eleven' skills; targets list corrected to opencode/claude/codex","skills/README.md: stale 'eka install skills|commands is the official installation path' claim replaced with the configure reality","README.md: added 'Mapping burden' subsection under The skill pack; refreshed stale 'future DELEGATION.txt sidecar' wording; DELEGATION.txt documented as reserved name; symlink refusal scoped to the final path component","skills/commands/README.md: --json added to configure examples (required flag); render-at-install wording made precise; DELEGATION.txt reserved-name note added","install.go: comment-only precision fixes (InstallForTarget, classifyTarget, writeFileScoped) — final-component symlink refusal vs followed intermediate directory symlinks","pack_test.go: TestRoleContractMatchesMappingTables + roleContractRoles helper (M3 drift guard)"],"role":"implementation","summary":"Packaging cleanup + consistency per req:agent-agnostic-skill-pack M2/M3 plus ratified wave-1/wave-4 review minors. M2a: manifest installCommand now cites the ADR-030 configure reality (eka-mcp configure --target opencode|claude|codex) instead of the removed eka install / ADR-023; skill list verified 11 manifest entries = 11 embedded eka-* dirs, all paths exist. M2b: skills/docs/installation.md rewritten to configure (--json required), stale 'ten' count fixed to eleven, dead .ekapack.json-marker and 'agents'-target claims dropped. M2c: README root gained a Mapping burden note (declarative mappings/*.toml -\u003e DELEGATION.txt at install time, no body editing) and the stale 'future sidecar' wording was refreshed. Wave-1 minors in skills/commands/README.md: configure examples now carry the REQUIRED --json flag; frontmatter-render claim made precise (rendered into installed copies at install time; canonical pack files byte-identical). Wave-4 minors: DELEGATION.txt documented as a RESERVED sidecar name (install overwrites it) in both READMEs where install is documented; symlink wording made precise everywhere (refusal covers the final path component only; intermediate directory symlinks are followed — dotfiles workflow), including the overstated comments in install.go (comment-only, no behavior change). M3: new TestRoleContractMatchesMappingTables in pack_test.go — robust approach chosen: RoleVocabulary == role tokens extracted from BOTH bodies' '## Role contract' sections (markdown-table first column), AND every mappings/*.toml table resolves exactly that vocabulary; fail-loudly verified by mutation in both directions (dropping a table row and renaming a body role each fail the test). Invariants held: zero alex-/althea- in skills/commands/*, description-only frontmatter, command bodies byte-unchanged, no behavioral code changes.","tests":["gates: GOCACHE=/tmp/opencode-marij/go-cache GOTMPDIR=/tmp/opencode-marij go build ./... \u0026\u0026 go vet ./... \u0026\u0026 go test ./... — all ok (github.com/maleolabs/eka-mcp, cmd/eka-mcp, internal/eka, internal/mcp)","TestRoleContractMatchesMappingTables — PASS; mutation-verified fail-loudly both directions (codex.toml row removal -\u003e loader refusal surfaces as FAIL; body role rename -\u003e vocabulary drift FAIL); worktree restored after mutation","count-consistency verification: ls -d skills/eka-*/ = 11 dirs; manifest.yaml lists 11 skill entries; all 21 manifest paths (skills+supporting) verified to exist on disk; docs claims aligned to eleven"]}