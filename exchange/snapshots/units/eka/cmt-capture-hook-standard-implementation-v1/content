{"changes":["Hooks pre-commit/pre-push via eka-standard + anvil platform_sync, eka capture --reconcile --from-git, provenance reconciled, 24h window, POSIX non-blocking."],"role":"implementation","summary":"Hooks pre-commit/pre-push via eka-standard + anvil platform_sync, eka capture --reconcile --from-git, provenance reconciled, 24h window, POSIX non-blocking.","tests":["go test ./... PASS","eka draft validate PASS"]}