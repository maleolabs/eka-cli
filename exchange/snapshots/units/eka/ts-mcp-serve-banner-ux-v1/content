{"description":"Upgrade the interactive serve notice of `eka mcp` / `eka mcp serve` (run directly on a terminal) to the eka CLI's visual language: a professional unicode banner plus state-derived relevant information, replacing today's plain-text minimal notice. The existing correctness contract is preserved exactly: notice goes to STDERR only, is gated on stdin-TTY detection (go-isatty), and non-TTY runs print absolutely nothing (stdout is the JSON-RPC channel — protocol framing must never be corrupted).","acceptanceCriteria":"1. Banner renders in the eka CLI visual language (section headers, tree glyphs like └──, aligned key-value rows; colors only when TTY, plain bytes otherwise) implemented LOCALLY in eka-mcp — no eka-cli import (A2 dependency direction holds).\n2. Content is state-derived and relevant: eka-mcp version, embedded pack name/version/status (from manifest), MCP protocol version (2024-11-05), transport line (stdio, JSON-RPC 2.0, newline-delimited), workspace block (EKA_HOME path, initialized yes/no, registered projects/repositories count; uninitialized renders a deterministic 'not initialized' state per req §3 decision 5, never a crash), capability counts (tools and resources exposed), and actionable hints (attach via `eka-mcp configure --target ... --json`; stop with Ctrl-C).\n3. Determinism: identical store/plugin state produces identical banner bytes; no wall-clock timestamps.\n4. Protocol safety unchanged: zero bytes written to stdout before/during serving; piped/non-TTY invocation remains fully silent; conformance suite still passes 100%.\n5. Tests: TTY-gated rendering (banner appears with simulated TTY, absent otherwise), content assertions per state (initialized vs uninitialized workspace), byte-determinism check.\n6. Full gates green in eka-mcp: go build ./..., go vet ./..., go test ./...","debtRationale":""}