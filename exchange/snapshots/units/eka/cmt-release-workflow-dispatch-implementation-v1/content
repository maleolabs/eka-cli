{"changes":[".github/workflows/release.yml: add workflow_dispatch trigger with required `version` input; version extraction prefers input and falls back to GITHUB_REF_NAME tag; header comment updated"],"role":"implementation","summary":"Add workflow_dispatch trigger to the Release workflow so a release can be triggered manually from the Actions UI or `gh workflow run` without deleting and re-pushing the tag. Version comes from a new required `version` input (without v prefix, must match anvil.yaml), falling back to the git tag on push trigger. No release pipeline logic changed beyond the trigger + input + version-source fallback.","tests":["manual: gh workflow run / Actions UI trigger","yaml parse check"]}