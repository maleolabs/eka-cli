{"changes":["eka-core workspace/project.go: new UnregisterProject(projectID) — deletes every repo row under the project and the project row itself; reports removed count; cleans orphaned empty project rows; canonical objects untouched","eka-core runtime/workspace.go: WorkspaceService.UnregisterProject passthrough (client-only boundary respected)","eka-cli cmd/project.go: unregister command — interactive confirm on TTY (default abort), --force required outside TTY, deterministic refusals with candidate listings","replaced the stale draft's raw-SQL branch and runtime+workspace double-open with the single registry operation","root cause of missing remove in help: stale installed binary, not a grouping bug — rebuild fixes"],"role":"implementation","summary":"Add eka project unregister \u003cproject\u003e [--force]: whole-project unregistration for deleted projects, moved paths, and cleanup.","tests":["eka-core workspace TestUnregisterProject: multi-repo removal, unknown project, orphan cleanup","eka-cli cmd: TestProjectUnregisterHappyPath (2 repos + sibling survival), RequiresForceOutsideTTY, UnknownProject, HelpExitsZero","manual E2E: register 2 repos, refuse without --force (exit 2), --force removes all, list empty after"]}