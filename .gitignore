# Built binary
/eka
/eka.exe

# Build output
/dist/
*.test
*.out

# Local EKA execution-state notes (dev-time, machine-specific; canonical
# state lives in the workspace store)
/.eka/

# EKA runtime transient state (sync staging, never committed)
**/exchange/.snapshots-tmp/
**/exchange/.snapshots-old/
